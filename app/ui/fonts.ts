import { Inter, Catamaran, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const catamaran = Catamaran({ subsets: ['latin'], weight: '900' });
export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });
